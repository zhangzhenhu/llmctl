diff --git a/vendor/genai/src/adapter/adapters/anthropic/streamer.rs b/vendor/genai/src/adapter/adapters/anthropic/streamer.rs
index 0d13320..88f3a9d 100644
--- a/vendor/genai/src/adapter/adapters/anthropic/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/anthropic/streamer.rs
@@ -233,6 +233,7 @@ impl futures::Stream for AnthropicStreamer {
 								captured_tool_calls: self.captured_data.tool_calls.take(),
 								captured_thought_signatures: None,
 								captured_response_id: None,
+								captured_provider_model_name: None,
 							};
 
 							// TODO: Need to capture the data as needed
diff --git a/vendor/genai/src/adapter/adapters/bedrock/streamer.rs b/vendor/genai/src/adapter/adapters/bedrock/streamer.rs
index f4484cc..0fe3345 100644
--- a/vendor/genai/src/adapter/adapters/bedrock/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/bedrock/streamer.rs
@@ -256,6 +256,7 @@ impl BedrockStreamer {
 			captured_tool_calls: self.captured_data.tool_calls.take(),
 			captured_thought_signatures: None,
 			captured_response_id: None,
+			captured_provider_model_name: None,
 		};
 		InterStreamEvent::End(end)
 	}
diff --git a/vendor/genai/src/adapter/adapters/cohere/streamer.rs b/vendor/genai/src/adapter/adapters/cohere/streamer.rs
index f801146..0cceeeb 100644
--- a/vendor/genai/src/adapter/adapters/cohere/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/cohere/streamer.rs
@@ -120,6 +120,7 @@ impl futures::Stream for CohereStreamer {
 										captured_tool_calls: self.captured_data.tool_calls.take(),
 										captured_thought_signatures: None,
 										captured_response_id: None,
+										captured_provider_model_name: None,
 									};
 
 									InterStreamEvent::End(inter_stream_end)
diff --git a/vendor/genai/src/adapter/adapters/gemini/streamer.rs b/vendor/genai/src/adapter/adapters/gemini/streamer.rs
index 0a9a1c9..4725de9 100644
--- a/vendor/genai/src/adapter/adapters/gemini/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/gemini/streamer.rs
@@ -47,6 +47,7 @@ impl GeminiStreamer {
 			captured_tool_calls: self.captured_data.tool_calls.take(),
 			captured_thought_signatures: self.captured_data.thought_signatures.take(),
 			captured_response_id: None,
+			captured_provider_model_name: None,
 		};
 		self.pending_events.push_back(InterStreamEvent::End(inter_stream_end));
 	}
diff --git a/vendor/genai/src/adapter/adapters/ollama/streamer.rs b/vendor/genai/src/adapter/adapters/ollama/streamer.rs
index 05b9bfe..5bc0035 100644
--- a/vendor/genai/src/adapter/adapters/ollama/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/ollama/streamer.rs
@@ -165,6 +165,7 @@ impl futures::Stream for OllamaStreamer {
 								captured_tool_calls: self.captured_data.tool_calls.take(),
 								captured_thought_signatures: None,
 								captured_response_id: None,
+								captured_provider_model_name: None,
 							};
 
 							return Poll::Ready(Some(Ok(InterStreamEvent::End(inter_stream_end))));
@@ -190,6 +191,7 @@ impl futures::Stream for OllamaStreamer {
 							captured_tool_calls: self.captured_data.tool_calls.take(),
 							captured_thought_signatures: None,
 							captured_response_id: None,
+							captured_provider_model_name: None,
 						};
 						return Poll::Ready(Some(Ok(InterStreamEvent::End(inter_stream_end))));
 					}
diff --git a/vendor/genai/src/adapter/adapters/openai/streamer.rs b/vendor/genai/src/adapter/adapters/openai/streamer.rs
index eb07921..fcc2dd2 100644
--- a/vendor/genai/src/adapter/adapters/openai/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/openai/streamer.rs
@@ -200,6 +200,7 @@ impl futures::Stream for OpenAIStreamer {
 							captured_tool_calls,
 							captured_thought_signatures: None,
 							captured_response_id: None,
+							captured_provider_model_name: self.captured_data.provider_model_name.take(),
 						};
 
 						return Poll::Ready(Some(Ok(InterStreamEvent::End(inter_stream_end))));
@@ -213,6 +214,12 @@ impl futures::Stream for OpenAIStreamer {
 							serde_error,
 						})?;
 
+					if let Ok(Some(model_name)) = message_data.x_take::<Option<String>>("model")
+						&& !model_name.is_empty()
+					{
+						self.captured_data.provider_model_name = Some(model_name);
+					}
+
 					if let Some(error) = take_stream_error(&mut message_data, &self.options.model_iden) {
 						return Poll::Ready(Some(Err(error)));
 					}
diff --git a/vendor/genai/src/adapter/adapters/openai_resp/streamer.rs b/vendor/genai/src/adapter/adapters/openai_resp/streamer.rs
index 9fd0b80..f1e6982 100644
--- a/vendor/genai/src/adapter/adapters/openai_resp/streamer.rs
+++ b/vendor/genai/src/adapter/adapters/openai_resp/streamer.rs
@@ -320,6 +320,7 @@ impl futures::Stream for OpenAIRespStreamer {
 								captured_tool_calls: self.captured_data.tool_calls.take(),
 								captured_thought_signatures: self.captured_data.thought_signatures.take(),
 								captured_response_id: Some(response.id),
+								captured_provider_model_name: Some(response.model),
 							};
 
 							return Poll::Ready(Some(Ok(InterStreamEvent::End(inter_stream_end))));
@@ -353,6 +354,7 @@ impl futures::Stream for OpenAIRespStreamer {
 								captured_tool_calls: self.captured_data.tool_calls.take(),
 								captured_thought_signatures: None,
 								captured_response_id: Some(resp_id),
+								captured_provider_model_name: Some(response.model),
 							};
 
 							return Poll::Ready(Some(Ok(InterStreamEvent::End(inter_stream_end))));
@@ -383,6 +385,7 @@ impl futures::Stream for OpenAIRespStreamer {
 							captured_tool_calls: self.captured_data.tool_calls.take(),
 							captured_thought_signatures: None,
 							captured_response_id: None,
+							captured_provider_model_name: None,
 						};
 						return Poll::Ready(Some(Ok(InterStreamEvent::End(inter_stream_end))));
 					}
diff --git a/vendor/genai/src/adapter/adapters/support.rs b/vendor/genai/src/adapter/adapters/support.rs
index eec55a7..6119d08 100644
--- a/vendor/genai/src/adapter/adapters/support.rs
+++ b/vendor/genai/src/adapter/adapters/support.rs
@@ -48,6 +48,7 @@ pub struct StreamerCapturedData {
 	pub reasoning_content: Option<String>,
 	pub tool_calls: Option<Vec<crate::chat::ToolCall>>,
 	pub thought_signatures: Option<Vec<String>>,
+	pub provider_model_name: Option<String>,
 }
 
 // endregion: --- Streamer Captured Data
diff --git a/vendor/genai/src/adapter/inter_stream.rs b/vendor/genai/src/adapter/inter_stream.rs
index 6b03a99..ef948f3 100644
--- a/vendor/genai/src/adapter/inter_stream.rs
+++ b/vendor/genai/src/adapter/inter_stream.rs
@@ -29,6 +29,9 @@ pub struct InterStreamEnd {
 
 	// Response ID for stateful sessions (OpenAI Responses API).
 	pub captured_response_id: Option<String>,
+
+	// Provider-reported model name captured at stream end when available.
+	pub captured_provider_model_name: Option<String>,
 }
 
 /// Intermediary StreamEvent
diff --git a/vendor/genai/src/chat/chat_stream.rs b/vendor/genai/src/chat/chat_stream.rs
index 5468009..bff9ceb 100644
--- a/vendor/genai/src/chat/chat_stream.rs
+++ b/vendor/genai/src/chat/chat_stream.rs
@@ -121,6 +121,9 @@ pub struct StreamEnd {
 
 	/// Response ID for stateful sessions (OpenAI Responses API).
 	pub captured_response_id: Option<String>,
+
+	/// Provider-reported model name captured at stream end when available.
+	pub captured_provider_model_name: Option<String>,
 }
 
 impl From<InterStreamEnd> for StreamEnd {
@@ -179,6 +182,7 @@ impl From<InterStreamEnd> for StreamEnd {
 			captured_content,
 			captured_reasoning_content: inter_end.captured_reasoning_content,
 			captured_response_id: inter_end.captured_response_id,
+			captured_provider_model_name: inter_end.captured_provider_model_name,
 		}
 	}
 }
@@ -192,6 +196,11 @@ impl StreamEnd {
 		captured_content.first_text()
 	}
 
+	/// Returns the provider-reported model name captured at stream end, if any.
+	pub fn captured_provider_model_name(&self) -> Option<&str> {
+		self.captured_provider_model_name.as_deref()
+	}
+
 	/// Consumes `self` and returns the first captured text, if any.
 	/// This is the concatenation of all streamed text chunks.
 	pub fn captured_into_first_text(self) -> Option<String> {
